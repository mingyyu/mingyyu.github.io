# Only needed to preview the site locally. GitHub Pages installs this set
# itself on push — nothing here has to be committed as a lockfile.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
